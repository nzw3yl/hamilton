<% if !current_user.nil? and current_user.admin? %>
  <div class="row">
	<div class="col-md-5">
	  <%= render 'todays_progress' %>
      <%= render 'postings' %>
	</div>
	<div class="col-md-6">
	  <%= render 'top_tags' %>	
	</div>
  </div>
<% else %>
  <%= render 'splash' %>
<% end %>
